filter {
    translate {
      override => true
      dictionary_path => '/etc/logstash/asa-5525-x.yaml'
      field => "host" # need to change this for other source applications.
      add_tag => [ "asa-5525-x" ]
    }
}
